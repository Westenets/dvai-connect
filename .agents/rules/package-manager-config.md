---
trigger: always_on
---

Use pnpm for all dependency management. Always run `pnpm i` instead of `npm install` or `yarn`. use `pnpm build` for building the code. to use any script in `package.json` use `pnpm <command>` instead of `npm` or `yarn`.
