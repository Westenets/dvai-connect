---
trigger: always_on
---

This project has 2 build commands in `package.json` scripts. Always use `build:dev` while testing or writing code in the current local system. the main `build` command is for production and will not be used here.